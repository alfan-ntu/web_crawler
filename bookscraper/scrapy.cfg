# Automatically created by: scrapy startproject
#
# For more information about the [deploy] section see:
# https://scrapyd.readthedocs.io/en/latest/deploy.html

[settings]
default = bookscraper.settings
#
# Add a new setting parameter to enable an interactive shell environment. This should be done manually
# after we did 'pip install ipython' so that 'scrapy shell' automatically picks ipython
#
shell = ipython

[deploy]
#url = http://localhost:6800/
project = bookscraper
